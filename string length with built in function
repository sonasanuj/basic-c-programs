#include <stdio.h>
#include <string.h>

int main()
{
    char str[20];
    int l;

    printf("enter a string: ");
    fgets(str, sizeof(str), stdin);

    l = strlen(str);
    if(str[l-1] == '\n')
        l--;

    printf("length of the string is: %d", l);

    return 0;
}
