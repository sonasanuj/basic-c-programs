#include<stdio.h>
int main()
{
    int num;
    printf("enter a non zero number:");
    scanf("%d",&num);
    if(num>=0)
        printf("number is +ve");
    else
        printf("number is -ve");
    return 0;
}
