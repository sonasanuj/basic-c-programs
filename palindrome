#include<stdio.h>
int main()
{
    int n,r,s=0,m;
    printf("enter a number:");
    scanf("%d",&n);
    while(n>0)
    {
        r=n%10;
        s=s*10+r;
        n=n/10;
        
    }
    if(m==s)
        printf("is a palindrome");
    else
        printf("not a palindrome");
}
